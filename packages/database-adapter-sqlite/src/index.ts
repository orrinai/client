import { SQLiteDatabaseAdapter } from './sqlite-database-adapter.js';

// Re-export the adapter as both named and default export
export { SQLiteDatabaseAdapter };
export default SQLiteDatabaseAdapter;
